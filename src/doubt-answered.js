1. //why onClick={()=> handleSearchedTitle} is working and onClick={()=> handleSearchedTitle()} is not ????
    // are you sure? I think should be the other way around.
2. // after refresh why question page is going to home page ???